using UnityEngine;
using System.Collections;

public class CLevel : MonoBehaviour {

	// Use this for initialization
    void makeSth() { 
//          GameObject prefab = GameSingelton.instance.sth;
//          GameObject clone = (GameObject)Instantiate(prefab, new Vector3(Random.Range(0, 10) * 2f, -4 + Random.Range(0, 2) * 0.5f, 0), transform.rotation);
//          clone.transform.localScale = new Vector3(0,0,0);
    }
    void Start () {
        makeSth();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
